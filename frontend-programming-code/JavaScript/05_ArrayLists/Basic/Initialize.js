// Inicializar mediante valor.
const lista1 = [1, "1", null, undefined, false];
const lista2 = new Array(1, "1", null, undefined, false);

// Inicializar indicando la longitud del mismo.
const lista3 = new Array(3);