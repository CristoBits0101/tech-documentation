// Si convertimos dos string a mayúsculas o minúsculas podemos compararlas.
console.log(text1.toLowerCase() === text2.toLowerCase()); // Minúsculas
console.log(text1.toUpperCase() === text2.toUpperCase()); // Mayúsculas