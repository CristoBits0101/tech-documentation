// Declarar un objeto.
const object_name = 
{
    ojos: "blues",
    pelo: `short`,
    edad: 17
}

object_name.edad;       
object_name["edad"];    

console.log
(
    object_name         // Acceder al objeto completo.
);

console.log
(
    object_name.edad    // Acceder al valor mediante la propiedad.
)

console.log
(
    object_name["edad"] // Acceder al valor mediante el índice.
)
