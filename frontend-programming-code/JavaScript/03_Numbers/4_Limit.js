// División infinita por 0.
let a = 1; 
let b = 0;
let c = a / b;

// Para saber los valores mínimos y máximos de JS.
let minimumPrecision = Number.EPSILON;
let minimumValue     = Number.MIN_VALUE;
let maximumValue     = Number.MAX_VALUE;