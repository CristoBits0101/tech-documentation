// Para exportar con ES6.
// Escribir -> "tipo": "módulo", (paquete.json).
export function sumThree (a, b, c) 
{
    d = a + b + c;
}

const variableName1 = (valueName1 => 3 * 2);

// 1 by file
export default variableName1;
export const book = "export a const with default";