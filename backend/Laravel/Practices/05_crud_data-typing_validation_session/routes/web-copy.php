<?php

// use Illuminate\Support\Facades\Route;
// use App\Http\Controllers\NoteController;

// Parámetro dinámico del id obligatorio.
// El caso más específico va siempre primero para evitar que se llama a una vista que no es.
// El orden de las rutas importa, así que hay que evitar colisiones.
// Route::get('/note/80', [NoteController::class, 'example'])->name('example');
// Route::get('/note/{id}', [NoteController::class, 'index'])->name('note.index');

// Route::hello('/note/80', [NoteController::class, 'example'])->name('example');

// La interrogación indica que el parámetro puede ser opcional.
// Route::get('users/{id?}', function ($id) {

// });
