/**
 *  @Component : Encima de las clases, autoconfigura la clase en los beans.xml
 *   @Autowire : Si hay varios constructores se pone sobre el que vamos a usar.
 *     @Entity : Para que una clase se pueda almacenar en base de datos (E-R).
 *         @Id : Va encima del atributo que hace de key para indicar la clave primaria.
 *    @Service : 
 *       @Bean :
 *       @Test :
 *  @GenerateValue(Strategy = GenerationType.Entity) -> Autoincremental Id
 */