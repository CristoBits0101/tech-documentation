/**
 *              Spring Web : MVC + Rest + Tomcat + JSON
 *         Spring Data JPA : JPA -> Hibernate -> JDBC
 *             H2 Database : In-memory database for development testing.
 *       PostgreSQL Drives : To be able to use PostgreSQL with Spring.
 *  SpringFox Boot Starter : Swagger as dependency.
 *          JSON web token : 
 *    Spring Boot DevTools : Auto-refresh of changes, without reloading or build the app.
 */