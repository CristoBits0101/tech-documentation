let minimum = Number.MIN_VALUE; // Valor mínimo de la variable
let maximum = Number.MAX_VALUE; // Valor máximo de la variable
let precision = Number.EPSILON; // Diferencia mínima de decimales comparables

console.log("Precisión mínima (Number.EPSILON):", minimumPrecision);
console.log("Valor mínimo (Number.MIN_VALUE):", minimumValue);
console.log("Valor máximo (Number.MAX_VALUE):", maximumValue);
