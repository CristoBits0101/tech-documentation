let a = 1;                      // Declaración.
let b = 0.111111;               // Declaración.
let c = 99.2323;                // Declaración.

console.log(Math.round(a + b)); // Redondea al entero más cercano.

console.log(c.toPrecision(2));  // Devuelve dos dígitos.

console.log(c.toFixed(2));      // Limita los decimales.
