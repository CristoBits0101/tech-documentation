// If
let charNum = '3'
let num1 = 3

if (charNum === num1) {
  // Code.
} else if (charNum == num1) {
  // Code.
} else {
  // Code.
}

// Switch
switch (charNum) {
  case '3':
    // Code.
    break
  case '2':
    // Code.
    break
  default:
    // Code.
    break
}

// Ternary
let isEqual = charNum === num1 ? 'Equal' : 'Not equal'
