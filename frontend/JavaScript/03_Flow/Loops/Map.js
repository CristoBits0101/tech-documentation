// Crea un nuevo array con los resultados de aplicar la función especificada a cada elemento del array original.

// Realizar alguna operación sobre cada elemento del array.
const array = [1, 2, 3, 4, 5];

const doubledArray = array.map((value) => value * 2);

console.log(doubledArray); // [2, 4, 6, 8, 10]