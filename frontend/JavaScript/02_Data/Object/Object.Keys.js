const obj = {
  name: "John",
  age: 30,
  profession: "Developer",
};

const keys = Object.keys(obj); // Devuelve un array con los claves del objeto.

console.log(keys);             // Array con todas las claves del objeto.
