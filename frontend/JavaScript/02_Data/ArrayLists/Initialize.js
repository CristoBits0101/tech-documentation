const lista1 = [1, "1", null, undefined, false];          // Inicializar mediante valor.
const lista2 = new Array(1, "1", null, undefined, false); // Inicializar mediante valor.
const lista3 = new Array(3);                              // Inicializar indicando la longitud.
